{
  "name": "@prompttracker/web",
  "version": "0.1.0",
  "private": true,
  "description": "Glassmorphic web dashboard for PromptTracker (React + Vite + vanilla CSS), bundled into @prompttracker/server's public directory",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc -p tsconfig.json",
    "build": "tsc -p tsconfig.json && vite build",
    "test": "tsc -p tsconfig.test.json && node --test dist/filters.test.js dist/format.test.js dist/App.test.js"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.14.9",
    "@types/react": "^18.3.5",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "typescript": "^5.5.2",
    "vite": "^5.4.8"
  },
  "optionalDependencies": {
    "@rollup/rollup-linux-x64-gnu": "4.24.0"
  }
}
