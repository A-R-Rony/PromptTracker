{
  "name": "prompttracker-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=@prompttracker/core && npm run build --workspace=@prompttracker/scanners && npm run build --workspace=@ar_rony1/prompt-lens && npm run build --workspace=@prompttracker/web",
    "build:all": "npm run build --workspace=@prompttracker/core && npm run build --workspace=@prompttracker/scanners && npm run build --workspace=@ar_rony1/prompt-lens && npm run build --workspace=@prompttracker/web",
    "dev": "npm run dev --workspace=@prompttracker/web",
    "dev:web": "npm run dev --workspace=@prompttracker/web",
    "test": "npm run test --workspace=@prompttracker/core && npm run test --workspace=@prompttracker/scanners && npm run test --workspace=@ar_rony1/prompt-lens && npm run test --workspace=@prompttracker/web"
  },
  "devDependencies": {
    "typescript": "^5.5.2"
  }
}
